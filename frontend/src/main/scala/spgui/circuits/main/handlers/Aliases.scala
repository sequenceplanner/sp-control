package spgui.circuits.main.handlers

import sp.domain.ID

object Aliases {
  type RunnerId = ID
  type OperationModelId = ID
  type VDModelId = ID
  type OperationId = ID
  type AbilityId = ID
  type VirtualDeviceId = ID
  type ResourceId = ID

  type VDModelName = String
  type DriverName = String
}
